# Base URL of the FastAPI backend. The browser talks to this directly, so this
# origin must appear in the backend's CORS_ORIGINS (see backend/.env.example).
NEXT_PUBLIC_API_URL=http://localhost:8000
